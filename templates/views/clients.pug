extends ../layouts/default

block content
  .container
    .row: col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
      if user
        h2 Clients
        if user.canAccessKeystone
          p.text-success This section is under construction...
          ul
            for client in clients
              li= client.fullName + " " + client._id
        else
          p.text-warning This section is under construction...
      else
        h2 Sorry, access denied.
        .alert.alert-danger
          strong Oops...
          br
          span You are not authorized.
    script.
      //console.log(locals);
      //console.log(data);
      //console.log(clients);
      //console.log(store);
